# k8s/users-service/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: users-service
  namespace: microservices
spec:
  type: NodePort  # ← Change from ClusterIP to NodePort
  selector:
    app: users-service
  ports:
  - port: 5000
    targetPort: 5000
    # nodePort: 30080  # Optional: specify fixed port (30000-32767)